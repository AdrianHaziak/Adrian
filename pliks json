#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main()
{
    string marka;
    string model;
    string id;
    string linia;

    ofstream wyjscie("b.json");
    ifstream wejscie;

    wej.open("a.txt");
    wyj.open("b.json");
    if(wejscie.good())
            while(!wej.eof()&&getline(wej, lina))
    {
        wej>>marka;
        wyj << " [\n{\n"marka":" "<<marka ;
        wej>>model;
        wyj << " [\n{\n"model":" "<<model;
        wej>>id;
        wyj << " [\n{\n"id":" "<<id;

    }

    wej.close();
    wyj.close();

    system("PAUSE");
    return 0;
}
